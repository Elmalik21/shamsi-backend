{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export DJANGO_SETTINGS_MODULE=shamsi_smart.settings.production && . /opt/venv/bin/activate && python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py seed_locations && python manage.py loaddata solar_data/fixtures/egyptian_electricity_tariffs.json || true && python manage.py loaddata solar_data/fixtures/solar_equipment_2026.json || true && gunicorn shamsi_smart.wsgi:application --workers 3 --bind 0.0.0.0:$PORT --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
