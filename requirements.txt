# ═══════════════════════════════════════════
# Shamsi Smart Backend — requirements.txt
# Python 3.12 | Railway / gunicorn
# ═══════════════════════════════════════════

# ── Django Core ───────────────────────────
Django==5.1.7
djangorestframework==3.15.2
django-cors-headers==4.4.0
django-filter==24.3
# drf-yasg==1.21.7  # REMOVED — incompatible with Python 3.12 (pkg_resources missing in Nix venv)

# ── Database ──────────────────────────────
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# ── Data Processing / ML ─────────────────
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.6.1
joblib==1.4.2
scipy==1.13.1

# ── Deployment ────────────────────────────
gunicorn==21.2.0
whitenoise==6.6.0
# python-decouple==3.8  # REMOVED — using os.environ directly for Railway compatibility

# ── Utilities ─────────────────────────────
requests==2.32.3
openpyxl==3.1.2
tqdm>=4.66.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# ── Deep Learning — CNN-LSTM inference + YOLOv8 roof detection ────────────────
# torch is imported lazily inside class methods so Railway boots without GPU.
# CPU-only wheels are much smaller (~200 MB vs 800 MB for CUDA).
torch>=2.0.0
torchvision>=0.15.0
ultralytics==8.0.200

# ── PDF export ────────────────────────────────────────────────────────────────
reportlab>=4.0.0
matplotlib>=3.8.0

# ── Computer Vision (roof image analysis) ────────────────────────────────────
opencv-python-headless>=4.8.0

# ── GeoTIFF / Satellite Imagery ───────────────────────────────────────────────
# Only used by offline data-pipeline scripts/ — NOT imported at runtime.
# Install locally when running scripts/:
#   pip install rasterio geopandas shapely pyproj earthpy
# rasterio>=1.3.0       # REMOVED from Railway — not needed at runtime, fails on Python 3.12 Nix
# geopandas>=0.14.0     # REMOVED from Railway — not needed at runtime
# shapely>=2.0.0        # REMOVED from Railway — not needed at runtime
# pyproj>=3.6.0         # REMOVED from Railway — not needed at runtime
# earthpy>=0.9.4        # REMOVED from Railway — not needed at runtime

# ── Sentinel-2 / Copernicus download ──────────────────────────────────────────
# Required for:  scripts/usgs_auto_download.py — local data collection only
# sentinelsat>=1.3.0  # REMOVED — no Python 3.12 wheel available

# ── Segment Anything Model (SAM) — auto-annotation ────────────────────────────
# Install with:
#   pip install git+https://github.com/facebookresearch/segment-anything.git
# segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
reportlab>=4.0.0
matplotlib>=3.8.0
