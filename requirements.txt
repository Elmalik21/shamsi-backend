# ═══════════════════════════════════════════
# Shamsi Smart Backend — requirements.txt
# Python 3.10+ | Railway / gunicorn
# ═══════════════════════════════════════════

# ── Django Core ───────────────────────────
Django==5.1.7
djangorestframework==3.15.2
django-cors-headers==4.4.0
django-filter==24.3
# drf-yasg==1.21.7  # REMOVED — incompatible with Python 3.12 (pkg_resources missing in Nix venv)

# ── Database ──────────────────────────────
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# ── Data Processing / ML ─────────────────
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
joblib==1.4.2
scipy==1.13.1

# ── Deployment ────────────────────────────
gunicorn==21.2.0
whitenoise==6.6.0
# python-decouple==3.8  # REMOVED — using os.environ directly for Railway compatibility

# ── Utilities ─────────────────────────────
requests==2.32.3
openpyxl==3.1.2
tqdm>=4.66.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# ── Deep Learning (optional — required for CNN-LSTM and YOLOv8 training) ──
# Uncomment or install separately if training locally:
# torch==2.0.1
# torchvision==0.15.2
# ultralytics==8.0.200

# ── AI Training extras ────────────────────────────────────────────────────
# These are needed for training but not for the production API server.
# Install with:  pip install torch==2.0.1 ultralytics==8.0.200 reportlab>=4.0.0
# torch==2.0.1
# ultralytics==8.0.200
reportlab>=4.0.0
matplotlib>=3.8.0

# ── GeoTIFF / Satellite Imagery (Phase 2 — YOLOv8 real data pipeline) ────────
# Required for:  scripts/extract_roofs_from_geotiff.py
#                scripts/download_sentinel2.py (validate mode)
#                scripts/semi_auto_annotate.py
# Windows users: install rasterio via conda or the unofficial binary wheel
#   conda install -c conda-forge rasterio geopandas shapely pyproj
#   pip install rasterio --extra-index-url https://wheelhouse.openquake.org/windows/
# Linux/Mac/Railway:  pip install rasterio geopandas shapely pyproj
rasterio>=1.3.0
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
earthpy>=0.9.4
opencv-python>=4.8.0

# ── Sentinel-2 / Copernicus download ──────────────────────────────────────────
# Required for:  scripts/usgs_auto_download.py
# Register free at: https://browser.dataspace.copernicus.eu/
# Add COPERNICUS_USERNAME and COPERNICUS_PASSWORD to your .env file

# ── Segment Anything Model (SAM) — auto-annotation ────────────────────────────
# Install with:
#   pip install git+https://github.com/facebookresearch/segment-anything.git
# Download model checkpoint:
#   python scripts/semi_auto_annotate.py --download-sam --sam-model-type vit_h
# segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
